[project]
name = "my-server-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "aaawwwws", email = "89956317+aaawwwws@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "py-cord>=2.6",
    "python-dotenv>=1.2.3",
]

[project.scripts]
my-server-bot = "my_server_bot.main:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
